// import { listObjects } from './storage';

describe('Storage', () => {
  beforeEach(() => {});

  afterEach(() => {
    jest.clearAllMocks();
  });

  it('should fetch and list objects from s3', () => {
    expect.assertions(1);

    expect(1).toEqual(1);
  });
});
